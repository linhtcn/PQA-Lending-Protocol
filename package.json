{
  "name": "defi-lending-protocol",
  "version": "1.0.0",
  "description": "DeFi Lending Protocol Dashboard with Hardhat smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "start": "./scripts/start-local.sh",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "fund": "hardhat run scripts/fundAccount.js --network localhost",
    "check-balance": "hardhat run scripts/checkBalance.js --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.19.0"
  }
}
